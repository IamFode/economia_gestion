round(c(bias.est.1,variance.est.1,mse.est.1,bias.est.2,variance.est.2,mse.est.2),4)
mean((theta.est.2-theta)^2
mean((theta.est.2-theta)^2)
mean((theta.est.2-theta)^2)
( bias.est.1=mean(theta.est.1-theta) )
( variance.est.1=var(theta.est.1) )
( mse.est.1=mean((theta.est.1-theta)^2) )
( bias.est.2=mean(theta.est.2-theta) )
( variance.est.2=var(theta.est.2) )
( mse.est.2=mean((theta.est.2-theta)^2) )
# all values in a row:
round(c(bias.est.1,variance.est.1,mse.est.1,bias.est.2,variance.est.2,mse.est.2),4)
# Eficiencia relativa entre los errores cuadráticos medios de dos estimadores
( mse.est.1=mean((theta.est.1-theta)^2) ) / ( mse.est.2=mean((theta.est.2-theta)^2) )
# Eficiencia relativa entre los errores cuadráticos medios de dos estimadores
( mse.est.2=mean((theta.est.2-theta)^2) ) / ( mse.est.1=mean((theta.est.1-theta)^2) )
par(mfrow=c(1,2))
hist(theta.est.1,freq=F,xlab="moments",main=paste("n=",n,sep=""))
abline(v=theta,lty=2,col="red",lwd=2)
hist(theta.est.2,freq=F,xlab="MLE",main=paste("n=",n,sep=""))
abline(v=theta,lty=2,col="red",lwd=2)
############################## Ejercicio 1 #####################################
x =  rnorm(20,mean=7,sd=2)
x
mean(x)
mean(x)
median(x)
############################## Ejercicio 1 #####################################
x =  rnorm(20,mean=7,sd=2)
mean(x)
median(x)
M = 10000
mu.est <- numeric(M)
mu = 7
n = 25
for (i in 1:M){
x <- rnrom(n,mean=mu,sd=2)
mu.est[i] <- mean(x)
}
############################## Ejercicio 1 #####################################
x =  rnorm(20,mean=7,sd=2)
mu = 7
n = 25
for (i in 1:M){
x <- rnrom(n,mean=mu,sd=2)
mu.est[i] <- mean(x)
}
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=2)
mu.est[i] <- mean(x)
}
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
M = 10000
mu.est <- numeric(M)
mu = 7
sigma = 2
n = 25
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
}
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
for (i in 1:M){
x <- rnorm(n,median=mu,sd=sigma)
mu.est[i] <- mean(x)
}
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
}
( bias.est=median(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=median((mu.est-mu)^2) )
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=median(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=median((mu.est-mu)^2) )
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=median(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=median((mu.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 25
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
}
for (i in 1:M){
y <- rnorm(n,mean=mu,sd=sigma)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 50
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
}
for (i in 1:M){
y <- rnorm(n,mean=mu,sd=sigma)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 100
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
}
for (i in 1:M){
y <- rnorm(n,mean=mu,sd=sigma)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 100
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 25
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
x =  rnorm(20,mean=7,sd=2)
mean(x)
median(x)
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 25
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 50
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 100
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 200
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 50
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 100
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 200
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
mean(x)
hist(x)
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 200
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
hist(x)
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 200
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
hist(x)
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 25
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
hist(x)
hist(x, bars=10)
?hist
hist(x, breaks=10)
hist(x, breaks=100)
hist(x, breaks=50)
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 25
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
hist(x, breaks=50)
M = 10000
mu.est <- numeric(M)
median.est <- numeric(M)
mu = 7
sigma = 1
n = 200
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
hist(x, breaks=50)
n = 500
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
hist(x, breaks=50)
hist(x, breaks=30)
hist(x, breaks=20)
n = 25
for (i in 1:M){
x <- rnorm(n,mean=mu,sd=sigma)
mu.est[i] <- mean(x)
median.est[i] <- median(x)
}
( bias.est=mean(mu.est-mu) )
( variance.est=var(mu.est) )
( mse.est=mean((mu.est-mu)^2) )
( bias.est=mean(median.est-mu) )
( variance.est=var(median.est) )
( mse.est=mean((median.est-mu)^2) )
# ¿Es la mediana de la muestra un estimador insesgado? ¿Por qué?
## Si, porque su sesgo es pequeño como también su varianza
"Para cada tamaño de muestra, calcule la eficiencia relativa entre los estimadores.
¿Qué concluirías? ¿Qué estimador tiene un mejor desempeño práctico?"
"El mejor estimador será para mean ya que su error cuadratico medio es menor"
# MSE_mean / MSE_median
( mse.est=mean((mu.est-mu)^2) ) / ( mse.est=mean((median.est-mu)^2) )
hist(x, breaks=20)
hist(x, breaks=10)
hist(x)
